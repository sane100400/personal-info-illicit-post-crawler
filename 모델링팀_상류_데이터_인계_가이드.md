# 모델링·실험팀 상류 데이터 인계 가이드

## 1. 문서 목적

이 문서는 다음 두 담당이 모델링·실험팀에 전달해야 하는 데이터와 검수 기준을 정의한다.

- `01 데이터수집-비식별화`: 공개 웹 후보 수집, 본문 추출, 페이지 상태 확인, 개인정보 마스킹
- `02 라벨링-데이터셋`: 요소별 라벨링, 합의 판정, 근거 표시, 중복 그룹 구성, 데이터 분할

모델링·실험팀은 이 인계 자료를 사용해 B3 직접 LLM 판정, O1 EvidenceGate, O2 선택적 LLM 재판정, 성능 비교, ablation 및 오류 분석을 수행한다.

## 2. 공통 원칙

### 2.1 보안 경계

모델링팀 전달 파일에는 다음 정보를 포함하지 않는다.

- 원 URL 또는 최종 도착 URL
- 마스킹 전 전화번호, 이메일, 메신저 아이디, 계정명
- 이름, 계좌번호, 주민등록번호 등 식별 가능한 개인정보
- 판매자 또는 게시자에게 연락할 수 있는 실제 정보
- 실제 유출 파일, 샘플 파일, 첨부파일, 신분증 이미지
- 로그인 쿠키, 세션, API 토큰, URL HMAC 비밀키

원 페이지 재검토가 필요한 경우 `sample_id`만 전달하고, 원 URL은 접근이 제한된 별도 시스템에서 데이터 담당자가 조회한다.

### 2.2 파일 규칙

| 항목 | 규칙 |
|---|---|
| 문자 인코딩 | CSV는 UTF-8 BOM, JSON/JSONL/Markdown은 UTF-8 |
| 행 식별자 | `sample_id`는 전체 데이터에서 유일해야 함 |
| 빈 값 | CSV는 빈 문자열, JSONL은 목적에 따라 `null` 또는 명시된 기본값 |
| 날짜 | ISO 8601 및 시간대 포함, 예: `2026-08-27T14:30:00+09:00` |
| 불리언 | 최종 JSONL에서는 `true/false` 또는 명세된 `0/1`만 사용 |
| 버전 | 데이터 버전, 라벨 가이드 버전, 생성 시각을 반드시 기록 |
| 무결성 | 최종 전달 파일마다 SHA-256을 제공 |
| 권한 | 민감한 연구 데이터 파일은 가능한 경우 파일 권한 `600` 사용 |

### 2.3 표준 마스킹 토큰

| 대상 | 토큰 |
|---|---|
| 전화번호 | `[PHONE]` |
| 이메일 | `[EMAIL]` |
| 메신저 아이디 | `[MESSENGER_ID]` |
| 연락 링크 | `[CONTACT_URL]` |
| 일반 URL | `[URL]` |
| 계정명 | `[ACCOUNT]` |
| 이름 | `[NAME]` |
| 계좌번호 | `[BANK_ACCOUNT]` |
| 주민등록번호 | `[NATIONAL_ID]` |
| IP 주소 | `[IP_ADDRESS]` |
| 기타 긴 숫자 식별자 | `[NUMERIC_IDENTIFIER]` |

한 종류의 정보에는 데이터 전체에서 동일한 토큰을 사용한다. 토큰 안에 원래 값의 일부를 남기지 않는다.

---

## 3. 01 데이터수집-비식별화 담당 인계 명세

### 3.1 요청 목적

모델링팀은 수집팀에 다음을 요청한다.

1. 마스킹된 제목과 실제 게시물 본문
2. 수집 성공·실패 및 본문 추출 상태
3. 페이지 유형과 현재 접근 가능 여부
4. 복제·유사 게시물 분석에 필요한 비식별 지문
5. 마스킹 검증 결과

검색어, 원 URL, 실제 연락처는 모델링팀에 전달하지 않는다.

### 3.2 필수 전달 파일

| 파일명 | 필수 | 내용 |
|---|---:|---|
| `candidates_masked.csv` | 예 | 마스킹된 수집 성공 후보 |
| `collection_log.csv` | 예 | 후보별 성공·실패·제외 사유 |
| `extraction_failures.csv` | 예 | 본문 추출 실패 및 부분 실패 사례 |
| `collection_summary.json` | 예 | 수집량과 안전 경계 요약 |
| `masking_validation_report.json` | 예 | 마스킹 잔여 패턴 검사 결과 |
| `data_manifest.json` | 예 | 버전, 생성 시각, 행 수, 파일 SHA-256 |
| 본문 추출 코드 및 설정 | 예 | 동일 결과 재생성을 위한 코드 버전과 설정 |

파일럿 단계에서는 먼저 30건을 전달해 라벨 화면과 필드 가독성을 확인하고, 수정 후 300건 전체를 전달한다.

### 3.3 `candidates_masked.csv` 필드

CSV의 물리적 값은 문자열이지만 아래 의미상 타입을 따른다.

| 필드 | 타입 | 필수 | 예시/제약 |
|---|---|---:|---|
| `sample_id` | string | 예 | `EG-000001`; 유일값 |
| `collected_at` | datetime | 예 | ISO 8601, 시간대 포함 |
| `source_type` | enum | 예 | `search`, `seed`, `manual` |
| `http_status` | integer/string | 예 | `200`, `404`, 빈 값 등 |
| `page_type` | enum | 예 | 3.4절 값 |
| `live_status` | enum | 예 | `accessible`, `inaccessible`, `unknown` |
| `extraction_status` | enum | 예 | 3.5절 값 |
| `masked_title` | string | 예 | 마스킹된 제목, 빈 값 가능 |
| `masked_text` | string | 예 | 마스킹된 본문 |
| `language_mix` | enum | 예 | `ko`, `en`, `ko_en_mixed`, `other` |
| `obfuscation_type` | string | 예 | `none` 또는 `+`로 연결된 유형 |
| `near_duplicate_fingerprint` | string | 예 | 예: `simhash64:...` |
| `campaign_group` | string | 권장 | 연락정보 기반 비식별 HMAC 또는 빈 값 |
| `final_url_hmac` | string | 내부 선택 | 원 URL이 아닌 HMAC; 모델 입력에서는 제외 |

#### 호환 필드명

현재 크롤러의 `near_duplicate_cluster`는 아직 실제 클러스터가 아니라 SimHash 지문이다. 인계 시 다음 중 하나로 처리한다.

- 권장: `near_duplicate_fingerprint`로 이름을 바꿔 의미를 명확히 한다.
- 기존 코드 호환: `near_duplicate_cluster`를 유지하되 manifest에 "fingerprint"임을 기록한다.

### 3.4 `page_type` 허용값

| 값 | 의미 |
|---|---|
| `original_post` | 작성자의 실제 원 게시물로 확인 |
| `search_result_list` | 검색결과 목록 |
| `search_reflection` | 정상 사이트가 검색어를 제목·본문에 반사한 페이지 |
| `news_or_education` | 뉴스, 보도자료, 교육·예방 자료 |
| `question_or_analysis` | 질문, 토론, 분석 글 |
| `deleted_or_inaccessible` | 삭제 또는 현재 접근 불가 |
| `unknown` | 자동·수동 확인으로 결정할 수 없음 |
| `other` | 위 유형에 속하지 않음 |

수집팀이 자동으로 부여한 값은 최종 정답이 아니라 `collector_page_type`이라는 예비 메타데이터로 취급한다. 라벨링팀이 별도로 최종 `page_type_label`을 판정한다.

현재 크롤러 출력과 표준값의 호환 매핑:

| 현재 크롤러 값 | 표준값 |
|---|---|
| `reflected_search_page` | `search_reflection` |
| `search_result_list` | `search_result_list` |
| `cached_or_deleted` | `deleted_or_inaccessible` |
| `news_or_education` | `news_or_education` |
| `unknown` | `unknown`; 자동으로 `original_post`로 바꾸지 않음 |

현재 크롤러의 `live_status="true"`는 수집 시점의 HTTP 접근 성공을 의미한다. 최종 `accessible_label`은 라벨링 단계에서 다시 확인한다.

### 3.5 `extraction_status` 허용값

| 값 | 의미 |
|---|---|
| `success` | 제목과 본문이 정상적으로 추출됨 |
| `partial` | 일부 본문만 추출됨 |
| `empty` | 본문이 비어 있음 |
| `boilerplate_only` | 메뉴·광고·푸터 등만 추출됨 |
| `dynamic_render_failure` | JavaScript 렌더링 문제로 본문 추출 실패 |
| `encoding_failure` | 문자 인코딩 문제 |
| `non_html` | HTML 본문이 아닌 자원 |
| `other_failure` | 기타 실패; 상세 사유 필수 |

실패 표본을 삭제하지 말고 `extraction_failures.csv`에 남긴다.

### 3.6 `collection_log.csv` 필드

| 필드 | 타입 | 설명 |
|---|---|---|
| `url_hmac` | string | 원 URL 대신 사용하는 비식별 식별자 |
| `query_group` | string | 공개하지 않는 실제 검색어가 아닌 내부 그룹명 |
| `outcome` | enum | `success`, `failed`, `skipped` |
| `http_status` | integer/string | 응답 상태 또는 빈 값 |
| `reason` | enum/string | 실패·제외 사유 |
| `attempted_at` | datetime | 시도 시각 |

권장 실패·제외 사유:

```text
robots_disallowed
robots_forbidden
dns_or_url_error
non_public_address
http_status
non_html_content
content_too_large
insufficient_text
dynamic_render_failure
timeout
redirect_error
masking_validation_failed
```

### 3.7 `masking_validation_report.json`

```json
{
  "dataset_version": "2026-08-27-pilot-v1",
  "checked_rows": 300,
  "residual_email_hits": 0,
  "residual_phone_hits": 0,
  "residual_national_id_hits": 0,
  "residual_http_url_hits": 0,
  "manual_reviewed_rows": 30,
  "manual_review_failures": 0,
  "passed": true,
  "generated_at": "2026-08-27T18:00:00+09:00"
}
```

`passed`가 `false`인 데이터는 라벨링팀이나 모델링팀에 전달하지 않는다.

### 3.8 수집팀 인수 기준

다음을 모두 만족해야 수집 산출물을 인수한다.

- [ ] `sample_id`가 비어 있지 않고 전체에서 유일함
- [ ] 원 URL과 실제 연락처가 모델링 전달 파일에 없음
- [ ] 마스킹 잔여 이메일·전화번호·URL 검사 결과가 0건임
- [ ] 수집 성공·실패·제외 건수의 합이 발견 후보 수와 일치함
- [ ] 본문 추출 실패 표본과 실패 이유가 보존됨
- [ ] 검색결과, 검색어 반사, 뉴스·교육, 삭제 페이지가 구분됨
- [ ] `near_duplicate_fingerprint` 또는 호환 SimHash 필드가 있음
- [ ] 코드 버전과 설정 버전이 기록됨
- [ ] 데이터 파일 SHA-256이 제공됨
- [ ] 파일럿 30건을 라벨링 화면에서 확인함

### 3.9 수집팀에 보낼 요청문

> 모델링·실험 인계를 위해 원 URL과 원 개인정보가 제거된 `candidates_masked.csv`, 수집 로그, 본문 추출 실패 목록, 수집 요약, 마스킹 검증 보고서 및 데이터 manifest를 요청드립니다. 표본별로 `sample_id`, 마스킹된 제목·본문, 페이지 유형, 접근 상태, 본문 추출 상태, 언어·난독화 정보, SimHash 지문을 포함해 주세요. 원 URL·실제 연락처·HMAC 비밀키는 전달하지 말아 주세요. 먼저 30건으로 필드와 화면을 검수한 후 파일럿 300건을 전달해 주세요.

---

## 4. 02 라벨링-데이터셋 담당 인계 명세

### 4.1 요청 목적

모델링팀은 라벨링팀에 다음을 요청한다.

1. 거래 의사·거래 대상·연락 방법의 요소별 정답
2. 페이지 유형·원본성·접근 가능 여부의 최종 정답
3. 최종 positive/negative/uncertain 라벨
4. 양성 표본의 세 요소별 근거 구간
5. hard negative 유형
6. 이중 라벨링·불일치·최종 합의 정보
7. 중복 그룹 누수가 없는 Train/Validation/Test 분할

### 4.2 필수 전달 파일

| 파일명 | 필수 | 내용 |
|---|---:|---|
| `label_guide_v1.0.md` 또는 PDF | 예 | 최종 판정 기준과 경계 사례 |
| `pilot_labels_200.csv` | 예 | 파일럿 참여자별 독립 라벨 |
| `label_disagreements.csv` | 예 | 불일치와 합의 근거 |
| `hard_negative_taxonomy.csv` | 예 | 음성 유형과 대표 합성 예시 |
| `dataset_labeled.jsonl` | 예 | 모델링용 최종 데이터 |
| `split_manifest.csv` | 예 | 표본·중복 그룹·split 매핑 |
| `dataset_card.json` | 예 | 데이터 구성과 품질 요약 |
| `label_quality_report.json` | 예 | 합의율, 불일치율, adjudication 현황 |
| `error_review_completed.csv` | 후속 필수 | FP/FN 100건 재검토 결과 |

### 4.3 독립 라벨 원본 형식

파일럿 및 본 라벨링의 개별 판단을 다음 형식으로 보존한다.

| 필드 | 타입 | 설명 |
|---|---|---|
| `sample_id` | string | 공통 표본 ID |
| `annotator_id` | string | 가명화된 라벨러 ID |
| `intent_label` | integer/null | `0`, `1`, 판단 불가 시 `null` |
| `trade_target_label` | integer/null | `0`, `1`, 판단 불가 시 `null` |
| `contact_label` | integer/null | `0`, `1`, 판단 불가 시 `null` |
| `page_type_label` | enum | 4.4절 값 |
| `page_original_label` | integer/null | `0`, `1`, 판단 불가 시 `null` |
| `accessible_label` | integer/null | `0`, `1`, 판단 불가 시 `null` |
| `explicit_negative_label` | integer/null | 명시적 오탐 유형이면 `1` |
| `final_label` | enum | `positive`, `negative`, `uncertain` |
| `negative_type` | enum/string | 음성 유형; 복수이면 `+` 연결 또는 JSON 배열 |
| `evidence_spans` | JSON object | 양성 요소별 근거 |
| `labeled_at` | datetime | 라벨 입력 시각 |
| `label_guide_version` | string | 예: `v1.0` |

### 4.4 `page_type_label` 허용값

```text
original_post
search_result_list
search_reflection
news_or_education
question_or_analysis
deleted_or_inaccessible
unknown
other
```

O1의 현재 최소 구현은 `page_original_label` 이진 예측을 사용한다. 논문에서 페이지 유형별 예측 결과를 보고하려면 `page_type_label`도 반드시 최종 데이터에 남긴다.

권장 파생 규칙:

```text
page_original_label = 1  if page_type_label == original_post
page_original_label = 0  otherwise
```

### 4.5 최종 요소 라벨과 판정 규칙

| 필드 | 타입 | 허용값 |
|---|---|---|
| `intent_label` | integer | 거래 의사 있음 `1`, 없음 `0` |
| `trade_target_label` | integer | 불법유통 거래 대상 해당 `1`, 비해당 `0` |
| `contact_label` | integer | 구체적 연락 방법 있음 `1`, 없음 `0` |
| `page_original_label` | integer | 원 게시물 `1`, 기타 `0` |
| `accessible_label` | integer | 내용 확인 가능 `1`, 불가능 `0` |
| `explicit_negative_label` | integer | 명시적 오탐 유형 `1`, 아니면 `0` |
| `final_label` | enum | `positive`, `negative`, `uncertain` |

최종 양성 판정은 다음과 일치해야 한다.

```text
positive =
    intent_label == 1
AND trade_target_label == 1
AND contact_label == 1
AND page_original_label == 1
AND accessible_label == 1
AND explicit_negative_label == 0
```

다음 경우에는 `uncertain`을 유지한다.

- 본문 추출 실패 또는 문맥 부족
- 페이지 상태를 확인할 수 없음
- 세 요소 중 하나 이상을 판단할 정보가 부족함
- 라벨 가이드만으로 합의할 수 없고 책임자 판정이 보류됨

`uncertain`을 학습 편의를 위해 임의로 positive 또는 negative로 바꾸지 않는다.

### 4.6 `negative_type` 표준값

| 값 | 정의 |
|---|---|
| `search_reflection` | 검색어가 정상 사이트에 반사된 페이지 |
| `search_result_list` | 검색결과 목록 |
| `single_own_account` | 본인 소유 단일 계정 거래 |
| `news_question_education` | 뉴스·질문·교육·분석 문맥 |
| `normal_db_context` | 정상 DB 개발·백업·마케팅 문맥 |
| `missing_intent` | 거래 의사 근거 부족 |
| `missing_target` | 불법유통 거래 대상 근거 부족 |
| `missing_contact` | 구체적인 연락 방법 부족 |
| `deleted_inaccessible` | 삭제·접근 불가 페이지 |
| `extraction_failure` | 제목·본문 추출 오류 |
| `slang_mixed_language` | 은어·초성·혼합언어로 판단이 어려움 |
| `other` | 기타; 설명 필수 |

음성 표본은 `negative_type`을 최소 하나 가져야 한다. 복수 원인이 있으면 JSON 배열 사용을 권장한다.

```json
["search_reflection", "missing_intent"]
```

현재 모델링 코드는 문자열도 처리하므로 초기 버전에서는 `search_reflection+missing_intent` 형식도 허용할 수 있다. 한 프로젝트 안에서는 한 형식만 사용한다.

### 4.7 `evidence_spans` 명세

positive 표본에는 거래 의사, 거래 대상, 연락 방법의 근거가 모두 있어야 한다.

권장 형식:

```json
{
  "intent": [
    {
      "field": "masked_text",
      "start": 0,
      "end": 6,
      "text": "판매합니다"
    }
  ],
  "trade_target": [
    {
      "field": "masked_text",
      "start": 8,
      "end": 13,
      "text": "고객 DB"
    }
  ],
  "contact": [
    {
      "field": "masked_text",
      "start": 18,
      "end": 32,
      "text": "[MESSENGER_ID]"
    }
  ]
}
```

offset 규칙:

- 마스킹된 `masked_title` 또는 `masked_text` 기준
- Unicode 문자열의 0-based character offset
- `start`는 포함, `end`는 미포함
- `text == source[field][start:end]`가 성립해야 함
- 근거가 여러 개이면 배열에 모두 기록
- 원문 개인정보를 `text`에 복원하지 않음

### 4.8 합의·최종 판정 필드

최종 데이터와 별도로 다음 정보를 보존한다.

| 필드 | 타입 | 허용값/설명 |
|---|---|---|
| `annotator_1_id` | string | 가명 ID |
| `annotator_2_id` | string | 가명 ID |
| `agreement_status` | enum | `agreed`, `disagreed`, `partial` |
| `adjudication_status` | enum | `not_needed`, `resolved`, `pending` |
| `adjudicator_id` | string | 책임자 가명 ID 또는 빈 값 |
| `adjudication_note` | string | 합의 근거; 실제 개인정보 금지 |
| `label_guide_version` | string | 판정에 사용한 가이드 버전 |

최종 모델 데이터에는 adjudicator 정보를 입력 특성으로 사용하지 않으며 재현성과 품질 감사에만 사용한다.

### 4.9 중복 그룹 명세

`duplicate_group`은 동일하거나 매우 유사한 광고 문구를 묶는 문자열이다.

권장 구성 기준:

- SimHash Hamming distance 3 이하
- 동일한 비식별 캠페인 HMAC
- 라벨러가 복제 글로 확인한 표본

그룹 ID 예:

```text
dup-a17c82fbd391
```

하나의 `duplicate_group`이 여러 split에 나타나면 데이터 인계를 거부한다.

### 4.10 데이터 분할 명세

| split | 권장 비율 | 사용 목적 |
|---|---:|---|
| `train` | 60% | 모델 학습 |
| `validation` | 20% | 임계값, O2 불확실 구간 결정 |
| `test` | 20% | 최종 성능 평가 전용 |

분할 조건:

- 같은 `duplicate_group`은 하나의 split에만 포함
- 각 split의 positive/negative 수를 보고
- validation과 test에도 충분한 positive가 포함되어야 함
- 임계값은 validation에서만 선택
- test 라벨은 최종 평가 전 동결
- B1/B2/B3/O1/O2가 동일한 test set을 사용
- 모델링팀 요청 없이 test set을 다시 나누거나 교체하지 않음

### 4.11 `dataset_labeled.jsonl` 필드

| 필드 | JSON 타입 | 필수 | 설명 |
|---|---|---:|---|
| `sample_id` | string | 예 | 유일 ID |
| `collected_at` | string | 예 | ISO datetime |
| `source_type` | string | 예 | 수집 방식 |
| `masked_title` | string | 예 | 마스킹 제목 |
| `masked_text` | string | 예 | 마스킹 본문 |
| `intent_label` | integer | 조건부 | `0/1`; uncertain은 `null` 허용 가능 |
| `trade_target_label` | integer | 조건부 | `0/1` |
| `contact_label` | integer | 조건부 | `0/1` |
| `page_type_label` | string | 예 | 최종 페이지 유형 |
| `page_original_label` | integer | 조건부 | `0/1` |
| `accessible_label` | integer | 조건부 | `0/1` |
| `explicit_negative_label` | integer | 조건부 | `0/1` |
| `negative_type` | string/array | 조건부 | negative일 때 필수 |
| `final_label` | string | 예 | `positive/negative/uncertain` |
| `evidence_spans` | object | 조건부 | positive일 때 세 요소 필수 |
| `duplicate_group` | string | 예 | 중복 그룹 ID |
| `split` | string | 예 | `train/validation/test` |
| `label_guide_version` | string | 예 | 예: `v1.0` |

모델링 코드 최소 호환 필드는 다음과 같다.

```text
sample_id
masked_title
masked_text
intent_label
trade_target_label
contact_label
page_original_label
accessible_label
explicit_negative_label
negative_type
final_label
duplicate_group
split
```

### 4.12 JSONL 예시

```json
{"sample_id":"EG-000001","collected_at":"2026-08-27T14:30:00+09:00","source_type":"search","masked_title":"고객 명단 판매","masked_text":"대량 판매합니다. 연락 [MESSENGER_ID]","intent_label":1,"trade_target_label":1,"contact_label":1,"page_type_label":"original_post","page_original_label":1,"accessible_label":1,"explicit_negative_label":0,"negative_type":"","final_label":"positive","evidence_spans":{"intent":[{"field":"masked_text","start":3,"end":8,"text":"판매합니다"}],"trade_target":[{"field":"masked_title","start":0,"end":5,"text":"고객 명단"}],"contact":[{"field":"masked_text","start":13,"end":28,"text":"[MESSENGER_ID]"}]},"duplicate_group":"dup-a17c82fbd391","split":"train","label_guide_version":"v1.0"}
```

### 4.13 `dataset_card.json` 필수 정보

```json
{
  "dataset_version": "2026-08-27-v1",
  "label_guide_version": "v1.0",
  "total": 1500,
  "positive": 300,
  "negative": 1150,
  "uncertain": 50,
  "train": {"total": 900, "positive": 180, "negative": 690, "uncertain": 30},
  "validation": {"total": 300, "positive": 60, "negative": 230, "uncertain": 10},
  "test": {"total": 300, "positive": 60, "negative": 230, "uncertain": 10},
  "duplicate_groups": 1200,
  "duplicate_group_leakage": 0,
  "masking_check_passed": true,
  "label_agreement": {
    "final_label_percent": 0.91,
    "cohen_kappa": 0.82
  },
  "sha256": "..."
}
```

### 4.14 라벨링팀 인수 기준

- [ ] 모든 표본에 유일한 `sample_id`가 있음
- [ ] positive/negative 표본의 요소 라벨이 모두 `0/1`임
- [ ] `final_label`과 EvidenceGate 요소가 모순되지 않음
- [ ] 모든 positive 표본에 세 요소의 근거 구간이 있음
- [ ] 근거 `text`와 해당 offset의 실제 문자열이 일치함
- [ ] 모든 negative 표본에 `negative_type`이 있음
- [ ] `uncertain`이 임의로 positive/negative로 변환되지 않음
- [ ] 두 라벨러의 독립 판정과 불일치 기록이 보존됨
- [ ] adjudication 미완료 표본이 확정 라벨로 들어가지 않음
- [ ] 같은 `duplicate_group`이 여러 split에 존재하지 않음
- [ ] Train/Validation/Test별 클래스 수가 보고됨
- [ ] test set과 라벨이 동결됨
- [ ] 데이터셋 버전과 SHA-256이 제공됨

### 4.15 라벨링팀에 보낼 요청문

> 모델링·실험을 위해 요소별 최종 라벨, 페이지 유형·원본성·접근 가능 여부, 최종 라벨, 음성 유형, 양성 근거 구간, 이중 라벨링·합의 기록을 요청드립니다. `duplicate_group` 단위로 Train 60%/Validation 20%/Test 20%를 나누고 동일 그룹이 여러 split에 포함되지 않도록 해 주세요. 모든 positive에는 거래 의사·거래 대상·연락 방법의 evidence span이 있어야 하며, 모든 negative에는 표준 `negative_type`이 있어야 합니다. `uncertain`은 임의 변환하지 말고 그대로 전달해 주세요. 최종 JSONL, split manifest, dataset card, 라벨 가이드 버전 및 파일 SHA-256을 함께 제공해 주세요.

---

## 5. 모델 오류 100건 재검토 인계

모델링팀은 동일 test set에서 선택한 최종 모델의 FP 50건과 FN 50건을 라벨링팀에 전달한다. 오류가 100건보다 적으면 가능한 전체 오류를 전달하고 부족 수를 보고한다.

### 5.1 모델링팀 전달 파일

`error_review_queue.csv`:

| 필드 | 타입 | 설명 |
|---|---|---|
| `sample_id` | string | 표본 ID |
| `model` | string | 오류를 낸 모델명 |
| `error_direction` | enum | `FP`, `FN` |
| `true_label` | enum | 기존 정답 |
| `prediction` | enum/integer | 모델 예측 |
| `masked_title` | string | 마스킹 제목 |
| `masked_text` | string | 마스킹 본문 |
| `original_negative_type` | string | 기존 유형 또는 빈 값 |

### 5.2 라벨링팀 작성 필드

| 필드 | 타입 | 허용값/설명 |
|---|---|---|
| `review_status` | enum | `confirmed`, `corrected`, `unresolved` |
| `corrected_label` | enum | `positive`, `negative`, `uncertain` |
| `error_source` | enum | `model`, `label`, `extraction`, `guide`, `multiple` |
| `error_category` | enum | 표준 오류 유형 |
| `reviewer_note` | string | 실제 개인정보를 제외한 설명 |
| `guide_change_needed` | boolean | 가이드 수정 필요 여부 |
| `synthetic_example` | string | 논문용 합성 예시 |

표준 오류 유형:

```text
search_reflection
slang_mixed_language
extraction_failure
single_own_account
news_question_education
normal_db_context
missing_intent
missing_target
missing_contact
deleted_inaccessible
label_error
other
```

완료 결과는 `error_review_completed.csv`와 오류 유형별 집계표로 전달한다.

---

## 6. 최종 인계 디렉터리 권장 구조

```text
handoff/
├── 01_collection/
│   ├── candidates_masked.csv
│   ├── collection_log.csv
│   ├── extraction_failures.csv
│   ├── collection_summary.json
│   ├── masking_validation_report.json
│   └── data_manifest.json
├── 02_labeling/
│   ├── label_guide_v1.0.md
│   ├── pilot_labels_200.csv
│   ├── label_disagreements.csv
│   ├── hard_negative_taxonomy.csv
│   ├── dataset_labeled.jsonl
│   ├── split_manifest.csv
│   ├── dataset_card.json
│   └── label_quality_report.json
└── 03_error_review/
    ├── error_review_queue.csv
    ├── error_review_completed.csv
    └── error_type_summary.csv
```

## 7. 모델링팀 최종 수령 체크리스트

- [ ] 수집 데이터에 원 URL과 원 개인정보가 없음
- [ ] 마스킹 검증 보고서가 통과 상태임
- [ ] 제목·본문·페이지·접근·추출 상태가 있음
- [ ] 요소별 라벨과 최종 라벨이 있음
- [ ] positive 근거 구간이 완전함
- [ ] negative 유형이 표준화되어 있음
- [ ] `duplicate_group`과 split이 있음
- [ ] 중복 그룹 누수가 0건임
- [ ] validation과 test의 양성 수가 충분함
- [ ] test set이 동결됨
- [ ] 데이터·가이드·코드 버전이 기록됨
- [ ] 각 파일의 SHA-256을 확인함
- [ ] 오류 100건 재검토 담당자와 반환 형식이 합의됨

위 조건 중 하나라도 충족되지 않으면 B3/O1/O2 최종 실험을 시작하지 않고 보완을 요청한다.
